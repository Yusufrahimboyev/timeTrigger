class Constants {
  const Constants._();
  static const String appName = 'Time Trigger';
  static const isLight="Theme.islight";
  static const isDark="Theme.isDark";
  static const locale="locale";
  static const baseUrl="";
  static const langCode = 'settings.locale';
}
class Urls{
  Urls._();

}